import type { Meta } from '@storybook/react';
import { Uikit } from './uikit';

const Story: Meta<typeof Uikit> = {
  component: Uikit,
  title: 'Uikit',
};
export default Story;

export const Primary = {
  args: {},
};
